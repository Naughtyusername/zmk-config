# Sweep (Cradio) ZMK Configuration

# Power Management
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000

# Combo support
CONFIG_ZMK_COMBO_MAX_COMBOS_PER_KEY=5
CONFIG_ZMK_COMBO_MAX_KEYS_PER_COMBO=2

# Enable OLED Display
CONFIG_ZMK_DISPLAY=y

# Standard Widgets
# Note: Layer and Output widgets only work on the Central (Left) side
CONFIG_ZMK_WIDGET_LAYER_STATUS=y
CONFIG_ZMK_WIDGET_BATTERY_STATUS=y
CONFIG_ZMK_WIDGET_BATTERY_STATUS_SHOW_PERCENTAGE=y
CONFIG_ZMK_WIDGET_OUTPUT_STATUS=y

# Power saving
CONFIG_ZMK_IDLE_TIMEOUT=30000

# Stronger Bluetooth signal (useful for split boards)
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
